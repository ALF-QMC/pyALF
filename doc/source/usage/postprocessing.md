(seq:pyalf_posprocess)=
# Postprocessing

The following sections demonstrate the postprocessing features in pyALF, each section can be executed individually, if QMC raw data from {numref}`seq:pyalf_comprun` is present.

```{tableofcontents}
```

%- Data collapse?
%- Fit exponential tau?
%If one wants to implement a custom postprocessing pipeline, starting with `alf_postprocess.py` and customizing it might be a suitable approach.
